const D=window.TRIP_DATA,S=D.meta.routeStatusLegend;
const e=s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));
const shortStatus=k=>({confirmed:"🟢 סגור",planned:"🟡 מתוכנן",decision:"🟠 דורש אימות מידע או נתונים",missing:"🔴 חסר",open:"⚪ פתוח"}[k]||S[k]||k);
const start=new Date("2026-09-25T15:05:00+03:00"),now=new Date(),diff=start-now,days=Math.max(0,Math.floor(diff/86400000)),hours=Math.max(0,Math.floor((diff%86400000)/3600000)),mins=Math.max(0,Math.floor((diff%3600000)/60000));if(countdownChip)countdownChip.textContent=diff>0?`${days} ימים · ${hours} שעות · ${mins} דקות`:"✈️ הטיול התחיל";if(window.countdownMini)countdownMini.textContent=diff>0?`עוד ${days} ימים`:"הטיול התחיל";
const confirmed=D.itinerary.filter(x=>x.transportStatus==="confirmed").length,open=D.itinerary.filter(x=>x.transportStatus!=="confirmed").length;confirmedCount.textContent=confirmed;openCount.textContent=open;
const flow=x=>{const steps=x.steps||[];if(!steps.length)return '';return `<div class="travel-flow"><strong>🧭 מה עושים עכשיו?</strong>${steps.map((s,i)=>`<div class="flow-step"><span>${i+1}</span><p>${e(s)}</p></div>`).join('')}${x.verify?`<div class="verify-box">🟠 <strong>דורש אימות מידע או נתונים:</strong> ${e(x.verify)}</div>`:`<div class="verified-box">🟢 המידע התפעולי במקטע זה מבוסס על ההזמנה הקיימת.</div>`}</div>`};
routeList.innerHTML=D.itinerary.map(x=>`<article class="route-card"><div class="top"><div class="date">${e(x.date)}</div><div class="status">${e(shortStatus(x.transportStatus))}</div></div><div class="route-grid"><div><div class="place">${e(x.from)}</div><div class="time">${e(x.departure)}</div></div><div class="arrow">&gt;</div><div><div class="place">${e(x.to)}</div><div class="time">${e(x.arrival)}</div></div></div><div class="meta"><div><strong>🚐 תחבורה</strong>${e(x.transport)}</div><div><strong>✨ מה עושים</strong>${e(x.activity)}</div>${x.hotel?`<div><strong>🏨 מלון</strong>${e(x.hotel)}</div>`:""}<div><strong>🧳 איפה המזוודה</strong>${e(x.luggage)}</div>${x.whyOpen?`<div><strong>ℹ️ למה לא סגור</strong>${e(x.whyOpen)}</div>`:""}<div><strong>📌 סטטוס פעילות</strong>${e(shortStatus(x.activityStatus))}</div></div>${flow(x)}${x.maps?`<div class="actions"><a target="_blank" rel="noopener" href="${e(x.maps)}">📍 נווט אותי</a></div>`:""}</article>`).join("");
routeStats.innerHTML=Object.keys(S).map(k=>{let n=D.itinerary.filter(x=>x.transportStatus===k).length;return n?`<span class="pill">${e(shortStatus(k))} ${n}</span>`:""}).join("");bookingList.innerHTML=D.bookings.map(b=>`<article class="booking-card"><div class="status">${e(shortStatus(b.status))}</div><h3>${e(b.name)}</h3><div>${e(b.type)}</div></article>`).join("");
const state=JSON.parse(localStorage.getItem("th_public_checklist")||"{}");checklistList.innerHTML=D.checklist.map((c,i)=>`<label class="row-card check-row"><input type="checkbox" data-i="${i}" ${state[i]?"checked":""}><span>${e(c.text)}</span></label>`).join("");document.querySelectorAll("#checklistList input").forEach(x=>x.onchange=()=>{let s=JSON.parse(localStorage.getItem("th_public_checklist")||"{}");s[x.dataset.i]=x.checked;localStorage.setItem("th_public_checklist",JSON.stringify(s))});
const q=id=>document.getElementById(id);const setNavActive=id=>{document.querySelectorAll('.bottom-nav button').forEach(b=>b.classList.remove('active','nav-active'));if(id==='home')q('homeBtn')?.classList.add('nav-active');else document.querySelector(`.bottom-nav button[data-tab="${id}"]`)?.classList.add('active')};const switchTab=id=>{document.querySelectorAll('.tab-panel').forEach(x=>x.classList.remove('active'));document.getElementById(id)?.classList.add('active');setNavActive(id);setTimeout(()=>window.scrollTo({top:document.querySelector('main').offsetTop-8,behavior:'smooth'}),20)};document.querySelectorAll('.bottom-nav button[data-tab]').forEach(b=>b.onclick=()=>switchTab(b.dataset.tab));q('homeBtn')?.addEventListener('click',()=>{document.querySelectorAll('.tab-panel').forEach(x=>x.classList.remove('active'));q('route')?.classList.add('active');setNavActive('home');window.scrollTo({top:0,behavior:'smooth'})});q('trackerBtn')?.addEventListener('click',()=>window.open('https://www.google.com/android/find/','_blank','noopener'));q('ticketsBtn')?.addEventListener('click',()=>switchTab('bookings'));q('hotelsBtn')?.addEventListener('click',()=>switchTab('bookings'));q('routeBtn')?.addEventListener('click',()=>switchTab('route'));q('documentsBtn')?.addEventListener('click',()=>switchTab('documents'));q('checkBtn')?.addEventListener('click',()=>switchTab('checklist'));q('linksBtn')?.addEventListener('click',()=>switchTab('useful'));q('alertsBtn')?.addEventListener('click',()=>switchTab('route'));q('updatesBtn')?.addEventListener('click',()=>switchTab('route'));